---
description: AI Dev Protocol project workflow entry
alwaysApply: true
---

# AI Dev Protocol

本规则是短入口。处理开发流程或 Apifox 请求时，从项目根目录读取 vendor/ai-dev-protocol/skills/ai-dev-protocol/SKILL.md，由 Router 分类并沿相对链接读取所需阶段。

必须同时安装 vendor/ai-dev-protocol/ 完整资源包。普通问答无需加载开发阶段。用户指令、项目业务与架构规则和宿主约束优先。

资源缺失时说明只有接入摘要，并提供补齐资源的安装说明；不要假定远端文件已进入上下文。不要重复安装同名 Router。
